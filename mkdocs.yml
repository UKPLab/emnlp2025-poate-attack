site_name: poate_attack
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: poate_attack
      handlers:
        python:
          options:
            docstring_style: numpy